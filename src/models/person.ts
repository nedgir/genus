export class Person {
  addedDate: string;
  acceptedDate: string;
  flags: number;
  identifier: string;
  name: string;
  declineReason: string;

  //additional info needed for Add invitees page
	status:any;
}
